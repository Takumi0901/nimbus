/* footer
--------------------------------------------------------- */
.l-footer {
  text-align: center;
  color: var(--colorDefaultLigten1);
  font-size: 10px;
  padding: 0 0 calc(var(--baseMargin) * 8);
}