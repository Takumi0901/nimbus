.c-label {
  display: inline-block;
  padding: 0.5em 1em;
  background: var(--colorDefaultLigten2);
  margin-left: calc(var(--baseMargin) / 2);
  border-radius: 4px;
  font-size: 1rem;
  
  &--highlight {
    background: var(--colorHighlight);
    color: var(--colorWhite);
  }
}